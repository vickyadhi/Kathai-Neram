class DashboardStoryPojo {
  String title, storyId, audio, story,thumbnail;
  List<dynamic> images;
  DashboardStoryPojo(this.title, this.storyId, this.audio, this.story, this.images,this.thumbnail);
}